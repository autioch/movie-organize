{
  "name": "mass-rename",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter=list test/**/*.spec.js & exit 0",
    "test:watch": "mocha --reporter=min --watch test/**/*.spec.js & exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.0.0",
    "eslint": "^3.19.0",
    "eslint-config-qb": "^1.0.0",
    "mocha": "^3.4.2"
  }
}
